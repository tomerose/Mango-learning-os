# V14.7.5 Reference Notes — Pro Quality Gate & Artifact Delivery

**Date**: 2026-06-08 | **Scope**: Quality enforcement, artifact document, PDF export

---

## 1. Quality Gate Tier Thresholds
**Reference**: Internal data from V14.7.2-V14.7.4 agent output analysis
**Pattern**: Pro/Admin 90, Standard 75, Guest 60 — with 9-dimension scoring
**MangoOS Adaptation**: Quality Gate v3 (`lib/agent/quality-gate-v3.ts`) implements this with auto-deepen for Pro/Admin below threshold
**Verdict**: ADAPT ✅

## 2. Browser Print for PDF
**Reference**: Standard web practice — `window.open()` + `document.write()` + `print()`
**Pattern**: Generate clean HTML in new window, trigger browser print dialog
**MangoOS Adaptation**: PDF export uses this approach — clean HTML with MangoOS branding
**Verdict**: ADAPT ✅ (no @react-pdf/renderer dependency needed)

## 3. Vercel AI SDK — useChat Status Enum
**Reference**: vercel/ai — ready/submitted/streaming/error status lifecycle
**Pattern**: Single status field drives all UI states
**MangoOS Adaptation**: Agent view state machine (templates/running/result/tasks) already mirrors this pattern. Deepen rounds add sub-states within "running".
**Verdict**: ADAPT (already aligned) ✅

## 4. shadcn/ui — Card + Alert Patterns
**Reference**: shadcn-ui/ui Card compound (CardHeader/CardContent/CardFooter), Alert destructive
**Pattern**: Cards for structured content, Alert for error/status messages
**MangoOS Adaptation**: Quality V3 breakdown card uses card-card pattern; source panel uses structured cards
**Verdict**: ADAPT ✅

## 5. DEV_FORCE_PLAN Development Override
**Reference**: Common Next.js pattern — env-based feature flags with NODE_ENV guard
**Pattern**: `process.env.DEV_FORCE_PLAN` only active when `NODE_ENV === "development"`
**MangoOS Adaptation**: Added to API route with development-only guard
**Verdict**: ADAPT ✅
